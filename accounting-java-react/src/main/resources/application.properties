# ===============================
# = DATA SOURCE (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/javaaccountingdb?useSSL=false&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.initialization-mode=always

# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ===============================
# = Flyway Migration (this section for migration from src/main/resources/db/migration/V1__create_users_table.sql)
# ===============================
#spring.flyway.enabled=false
#spring.flyway.locations=classpath:db/migration
#spring.flyway.baseline-on-migrate=true

# ===============================
# = JWT Security (custom property)
# ===============================
security.jwt.secret=my-super-secret-key
security.jwt.expiration=3600000   # 1 hour in ms

# ===============================
# = Server / Logging
# ===============================
server.port=8080
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# JWT secret must be Base64 encoded and >= 256 bits
jwt.secret=TXlTdXBlclNlY3VyZVNlY3JldEtleTEyMzQ1Njc4OTEyMzQ1Njc4OTAxMjM=

#Jackson will automatically map between:
#first_name <--> firstName
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jpa.open-in-view=false

# Enable multipart handling (default is true, but safe to set)
spring.servlet.multipart.enabled=true

# Maximum file size per uploaded file
spring.servlet.multipart.max-file-size=20MB

# Maximum size of the entire request (all files + fields)
spring.servlet.multipart.max-request-size=20MB
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=TRACE

